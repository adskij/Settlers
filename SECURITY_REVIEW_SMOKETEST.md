# Security review smoke test (run 2)

Temporary file to verify the `Security Review` GitHub Action runs on pull
requests and posts findings now that `ANTHROPIC_API_KEY` is configured.
Safe to delete once confirmed.
