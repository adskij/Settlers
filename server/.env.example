# Copy to server/.env and adjust as needed.
PORT=4000
# Change this to a long random string in production.
JWT_SECRET=dev-insecure-secret-change-me
# SQLite database file location.
DATABASE_PATH=./data/settlers.db
