# Copy to .env for docker-compose. Compose reads this file automatically.
# Generate a strong secret, e.g.:  openssl rand -hex 32
JWT_SECRET=change-me-please-set-a-real-secret
# Host port to expose the app on (container always listens on 4000).
PORT=4000
